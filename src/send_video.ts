export const send_video = (
    video: string,
    chat_ids: string[],
    caption: string
) => {
    console.log(video)
    console.log(caption)
    console.log(chat_ids)
}

export const mess_send_video = () => {}
