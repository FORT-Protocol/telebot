export const send_photo = (
    token: string,
    photo: string,
    chat_ids: string[],
    caption: string
) => {
    console.log(token)
    console.log(photo)
    console.log(caption)
    console.log(chat_ids)
}

export const mess_send_photo = () => {}
