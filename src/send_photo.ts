export const send_photo = (
    photo: string,
    chat_ids: string[],
    caption: string
) => {
    console.log(photo)
    console.log(caption)
    console.log(chat_ids)
}

export const mess_send_photo = () => {}
